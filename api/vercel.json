{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/dashboard/(.*)", "dest": "/api/dashboard" },
    { "src": "/(.*)", "dest": "/index.js" }
  ]
}
